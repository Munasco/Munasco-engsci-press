The sample main.c code has been pre-compiled for you.
You may run (1): 

	gcc -o snek_AI main.o snek_api.o
	./snek_AI

To test it out.
As you modify main.c, you will need to recompile using (2):
	
	gcc main.c -o main.o -c

Then re-link using (1).
